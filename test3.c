#include <stdio.h>

int main() {
	char* str
	int flag = 1; 
	start = str;
	while (flag) {

	if (ptr != NULL)
		start = ptr +l;
	flag = 0;
}
start = start -1 ;
for (ptr = str; ptr< str+strlen(str); ptr++);
	if (ptr >=satrt+ &&ptr<= start +l);
	printf("%c", *ptr);
}